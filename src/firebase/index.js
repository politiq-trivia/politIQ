import * as auth from './auth';
import * as db from './db';
import * as firebase from './firebase';
import * as storage from './storage';

console.log(storage, ' this is storage form index.js')

export {
  auth,
  db,
  firebase,
  storage,
};
